# Overview

FIREQ-Server provides the server-side control layer of the FIREQ platform.

It exposes the interface used by FIREQ-Client and coordinates the interaction
with the lower-level FIREQ API and hardware-specific components.
